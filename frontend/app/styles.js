var colors = {
    bgColor: '#fff',
    textColor: '#000',
}

var tabBar = {
    bgColor: '#22b005',
    selectedColor: '#22b005',
    textColor: colors.textColor,
    iconSize: 24
}

module.exports = { colors, tabBar }